# Shared Package (`packages/shared`)

Shared constants and utilities used across backend/workers.

## Contents

- `gim_shared/constants.py` - taxonomy/constants shared by API and workers

## Tests

The repository currently does not include a dedicated `packages/shared/tests` suite. Shared behavior is exercised through backend and worker tests.
