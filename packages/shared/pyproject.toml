[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "gim-shared"
version = "0.1.0"
description = "Shared utilities and scoring formulas for IssueIndex"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8.3.4",
]

[tool.hatch.build.targets.wheel]
packages = ["gim_shared"]
