# Copy to .env.local and fill values.

DATABASE_URL=
DIRECT_DATABASE_URL=
REDIS_URL=

ENVIRONMENT=development
LOG_LEVEL=INFO
CORS_ORIGINS=http://localhost:3000
FRONTEND_BASE_URL=http://localhost:3000
EMBEDDING_MODE=nomic

# Generate: python3 -c "import secrets; print(secrets.token_urlsafe(48))"
FINGERPRINT_SECRET=

# Generate: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=

GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

GIT_TOKEN=

GCP_PROJECT=
GCP_REGION=us-central1
CLOUD_TASKS_QUEUE=profile-jobs
EMBED_WORKER_URL=
RESUME_WORKER_URL=

PUBSUB_PROJECT=
PUBSUB_ISSUES_TOPIC=issueindex-issues
PUBSUB_PROFILES_TOPIC=issueindex-profiles
PUBSUB_DLQ_TOPIC=issueindex-dlq

EMBEDDING_MODEL=nomic-embed-text-v2-moe
EMBEDDING_DIM=256
EMBEDDING_BATCH_SIZE=25
MAX_CONCURRENT_EMBEDDINGS=4

SESSION_REMEMBER_ME_DAYS=7
SESSION_DEFAULT_HOURS=24
MAX_SESSIONS_PER_USER=5
# Shared cookie domain for frontend/API.
# Production: ".issueindex.dev". Empty for local dev.
COOKIE_DOMAIN=
MAX_AUTH_REQUESTS_PER_MINUTE=10
RATE_LIMIT_WINDOW_SECONDS=60

FEED_FRESHNESS_HALF_LIFE_DAYS=7.0
FEED_FRESHNESS_WEIGHT=0.25
FEED_FRESHNESS_FLOOR=0.2
FEED_DEBUG_FRESHNESS=false
SEARCH_FRESHNESS_HALF_LIFE_DAYS=7.0
SEARCH_FRESHNESS_WEIGHT=0.25
SEARCH_FRESHNESS_FLOOR=0.2

RECO_FLUSH_SECRET=
RECO_EVENTS_FLUSH_BATCH_SIZE=1000
RECO_FLUSH_MAX_SECONDS=60

# Frontend reads these from root .env.local
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
NEXT_PUBLIC_MOCK_API=false
